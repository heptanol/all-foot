import {Component, Input} from '@angular/core';
import {TableTeam} from '../model';

@Component({
  selector: 'app-sprite-logo',
  templateUrl: './sprite-logo.component.html'
})
export class SpriteLogoComponent {
}

